PORT=
NODE_ENV=
MONGODB_URI=
JWT_SECRET=
JWT_EXPIRE=30d
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET_NAME=
# Required for PDF uploads — all files are stored in S3 only (no local disk storage)
